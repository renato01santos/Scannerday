const { requireAdmin } = require("./_lib/admin-auth");
const { supabase } = require("./_lib/supabase");
module.exports=async function handler(request,response){if(request.method!=="GET")return response.status(405).json({error:"Método não permitido"});try{await requireAdmin(request);const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,userResponse=await fetch(`${url}/auth/v1/admin/users?page=1&per_page=1000`,{headers:{apikey:key,Authorization:`Bearer ${key}`}});if(!userResponse.ok)throw new Error("Não foi possível carregar os usuários");const auth=await userResponse.json(),profiles=await supabase("profiles?select=id,name,plan,role,created_at"),byId=new Map(profiles.map(p=>[p.id,p]));return response.status(200).json((auth.users||[]).map(user=>({...byId.get(user.id),id:user.id,email:user.email,created_at:user.created_at,confirmed_at:user.confirmed_at,last_sign_in_at:user.last_sign_in_at})));}catch(error){return response.status(error.statusCode||500).json({error:error.message});}};
